# Required by `scan`, `curate`, `aggregate` and `publish`.
YOUTUBE_API_KEY=

# Required by `publish` only, which authenticates via OAuth to write playlists.
# Scanning reads public data and does not need these.
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
